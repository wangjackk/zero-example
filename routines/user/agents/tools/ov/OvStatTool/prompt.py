PROMPT = """
Use this tool to get metadata (stat) for a viking:// URI,
such as type (file/directory), size, creation and modification timestamps,
and any tags stored with the resource.

Returns a structured key-value text.
"""

DESCRIPTION = (
    '查询 OpenViking 记忆卡片/目录的元信息（类型、大小、创建/更新时间等）。'
)
