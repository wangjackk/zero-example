PROMPT = """
Use this tool to list the memory tree (files and subdirectories)
under a given viking:// URI path, recursively.

Returns a tree-like text output similar to unix `tree`.
"""

DESCRIPTION = (
    '查看 OpenViking 指定目录下的记忆树结构（递归列出）。'
)
