PROMPT = """
Use this tool to search the long-term memory system (OpenViking) semantically.
Pass a natural-language query describing what you want to recall,
optionally a viking:// URI to scope the search,
and optionally a max result count (limit, default 10).

Returns a ranked list of memory card summaries.
"""

DESCRIPTION = (
    '在 OpenViking 记忆系统中做语义检索，查找相关的记忆卡片。'
)
