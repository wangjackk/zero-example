PROMPT = """
Use this tool to read memory card content at a specific level (L0/L1/L2).
L0 = short summary (title + one line),
L1 = medium summary,
L2 = full card content.

Returns the card text at the requested level.
"""

DESCRIPTION = (
    '按层级读取 OpenViking 记忆卡片内容（L0/L1/L2）。'
)
